# chat app

## Installation

1. Clone this repo. We prefer `SSH` rather than `HTTPS`
2. Configure environment variables

   1. Create `.env` file

      ```bash
      cp .env.example .env
      ```

3. Install dependencies

   ```bash
   npm install
   ```

4. Run server

   ```bash
   npm start
   ```
